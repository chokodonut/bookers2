<h1>User info</h1>

  <%= image_tag current_user.get_profile_image(100,100) %>

<tr>
  <tb>name</tb>
  <tb>introduction</tb>
<%= current_user.name %>
<!--<%= @user.name %>-->
</tr>
<h1>New book</h1>
  <%= form_with model: @book do |f| %>
    <h4>Title</h4>
      <%= f.text_field :title %>

    <h4>Opinion</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>

  <%= link_to "botn", edit_user_path(@user.id) %>

  <h1>Books</h1>

  <% @books.each do |book| %>
  <!--viewだけで書くなら@user.books-->

  <p>Title</p>
  <%= book.title %>

  <p>Opinion</p>
  <%= book.body %>

  <% end %>
